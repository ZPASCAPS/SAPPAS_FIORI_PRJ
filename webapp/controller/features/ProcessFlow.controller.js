/**
 * ProcessFlow.controller.js
 *
 * 역할:
 * - Sales Distribution(채널별 재고 분포) 차트 영역을 담당한다.
 */
sap.ui.define([
    "sap/ui/core/mvc/Controller"
], function (Controller) {
    "use strict";

    return Controller.extend("com.capstone.dashboard.fioridashboard.controller.features.ProcessFlow", {});
});
